# Text Helper MVP

Local install:

1. Open `chrome://extensions`.
2. Enable Developer mode.
3. Click `Load unpacked`.
4. Select this folder.

Boundary:

This sample only reads user-selected page text. It does not bypass login, scrape private data, or automate platform actions.

